## 1. 輸入邏輯調整

- [x] 1.1 檢視 `core/src/main.zig` 的 `handleStdinInput` 與 stdin polling，確認目前無換行時的處理分支
- [x] 1.2 在 stdin 為 TTY 且緩衝區無換行時，新增單鍵 `q` 立即退出的判斷並僅消耗該 byte
- [x] 1.3 確保非 `q` 的單字元不會被吞掉，仍交由既有逐行/JSON 解析流程處理

## 2. 行為一致性

- [x] 2.1 驗證計時進行中與結束後都會走相同退出路徑（`ipc.sendExit` + flush）
- [x] 2.2 確認非 TTY/管線輸入時維持行輸入語意，不觸發單鍵退出

## 3. 驗證

- [x] 3.1 手動測試：倒數進行中按下 `q` 立即退出
- [x] 3.2 手動測試：倒數結束後按下 `q` 立即退出
